{
  "name": "logseq-todoist-plugin",
  "version": "1.0.0",
  "author": "hkgnp",
  "description": "Simple Todoist plugin to pull only active tasks from specified project, i.e. Inbox",
  "logseq": {
    "id": "logseq-todoist-plugin-ts",
    "title": "logseq-todoist-plugin-ts"
  },
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "default": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build --no-source-maps index.html --public-url ./"
  },
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "0.0.1-alpha.29",
    "axios": "^0.24.0"
  },
  "devDependencies": {
    "eslint": "^8.1.0",
    "parcel": "^2.0.0",
    "typescript": "^4.4.4"
  }
}
